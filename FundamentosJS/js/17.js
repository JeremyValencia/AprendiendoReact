// Condicionales

const disponible = 4000
const retirar = 200

if( disponible > retirar){
    //Se cumple la condicion y se ejecuta el codigo
    console.log('Se puede retirar el dinero')
}else{
    //No se cumple la condicion y se ejecuta este codigo
    console.log('No se puede retirar el dinero')
}

/**
 * > Mayor que
 * < Menor que
 * >= Mayor o igual que
 * <= Menor o igual que
 */